{
  "name": "@kozou/benchmarks",
  "version": "0.0.0",
  "private": true,
  "description": "Reproducible benchmark: AI agent accuracy and cost on business questions across database-context arms (raw DDL vs generic MCP+comments vs Kozou), on a legacy-named synthetic schema at three scales",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run",
    "bench": "tsx src/run.ts",
    "emit-schema": "tsx src/schema/emit.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "kozou": "workspace:*",
    "@kozou/test-utils": "workspace:*",
    "@testcontainers/postgresql": "^12.1.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0"
  }
}
