{
  "name": "@kozou/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Kozou MCP server (stdio + HTTP) for AI agents (skeleton; v0.1 implementation pending).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  }
}
