{
  "name": "@kozou/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Kozou MCP server for AI agents (stdio; HTTP は v0.1.1)",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "kozou-mcp": "./src/cli.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@kozou/core": "workspace:*",
    "@kozou/introspect": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0"
  }
}
